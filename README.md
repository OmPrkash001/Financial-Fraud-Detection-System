# Financial Fraud Detection System 🛡️

A sophisticated Machine Learning application designed to detect fraudulent financial transactions in real-time. This project leverages **XGBoost** for high-accuracy predictions and features a premium **Glassmorphism UI** deployed via a Python Flask backend.

## 🚀 Key Features

-   **High Accuracy**: Trained on transaction data using XGBoost Classifier.
-   **Real-time Analysis**: Instant feedback on transaction safety.
-   **Premium UI**: A modern, responsive interface built with HTML5, CSS3, and JavaScript.
-   **Unified Deployment**: Runs a single local server for both backend API and frontend UI.
-   **Firebase Ready**: Architected for easy deployment to Firebase Hosting & Cloud Functions.

## 🛠️ Tech Stack

-   **Language**: Python 3.11
-   **Frontend**: HTML, CSS (Glassmorphism), JavaScript (Fetch API)
-   **Backend**: Flask (Python)
-   **ML Model**: XGBoost, Scikit-Learn, Pandas
-   **Environment**: Virtual Environment (venv)

## 📦 Installation

Prerequisites: Python 3.11 must be installed.

1.  **Clone the Repository**:
    ```bash
    git clone <repository-url>
    cd <repository-folder>
    ```

2.  **Run the Setup Script** (Windows):
    Simply double-click **`run_project.bat`**.

    *This script automatically:*
    -   Activates the virtual environment.
    -   Starts the Python Flask backend.
    -   Opens your default web browser to the application.

## 💻 Manual Setup (If Script Fails)

1.  **Create Virtual Environment**:
    ```bash
    python -m venv venv
    .\venv\Scripts\activate
    ```

2.  **Install Dependencies**:
    ```bash
    pip install -r firebase_app/functions/requirements.txt
    ```

3.  **Run the Application**:
    ```bash
    cd firebase_app/functions
    python main.py
    ```
    Access the app at `http://127.0.0.1:5000`.

## 📂 Project Structure

```text
├── firebase_app/
│   ├── functions/          # Python Backend (Flask)
│   │   ├── main.py         # API & Static File Server
│   │   ├── requirements.txt
│   │   └── *.pkl           # ML Model Artifacts
│   └── public/             # Frontend Assets
│       ├── index.html      # Main user interface
│       ├── style.css       # Premium styling
│       └── script.js       # Frontend logic
├── run_project.bat         # One-click startup script
├── train.py                # Model training script
├── generate_dummy_data.py  # Data generation utility
└── requirements.txt        # Project dependencies
```

## 🤖 Model Details

The model is trained to predict the probability of fraud based on:
-   **Transaction Amount**
-   **Category** (Grocery, Entertainment, etc.)
-   **Demographics** (Age, Gender, Job)
-   **Time & Location** (Hour, State)

*Note: The current version uses synthetic data generated by `generate_dummy_data.py` for demonstration purposes.*

---
**Developed by [Your Name]**
